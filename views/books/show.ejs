<h2><%= book.title %></h2>
<img src="<%= book.coverImagePath %>" alt="" width="100" height="150">

<div>
    <% if (book.author) { %>
      Author: <%= book.author.name %>
    <% } else { %>
      Author: Unknown
    <% } %>
  </div>
<div>Publish Date: <%= book.publishDate.toDateString() %></div>
<div>Page Count: <%= book.pageCount %></div>
<div>Description: <%= book.description %></div>

<a class="btn btn-primary" href="/books/<%= book.id %>/edit">Edit</a>
<%- include('../partials/deleteForm', {url:`/books/${book.id}`}) %>
<a class="btn btn-primary" href="/authors/<%= book.author ? book.author.id : '' %>">View Author</a>
